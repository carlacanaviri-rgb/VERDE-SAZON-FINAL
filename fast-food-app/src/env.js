// Este archivo es generado en build time por Render usando variables de entorno.
// NUNCA pongas valores reales aquí — este archivo va al repositorio vacío.
(function (window) {
  window.__env = window.__env || {};
  window.__env.MAPS_API_KEY = '$MAPS_API_KEY';
})(this);
